{
  "name": "triomphe-remates",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "install-server": "cd server && npm install",
    "build": "cd client && npm install && npm run build && cp -r dist ../server/client && cd ../server && npm install"
  }
}
