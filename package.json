{
  "name": "triomphe-remates",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install --include=dev && npm run build && cp -r dist ../server/client && cd ../server && npm install --omit=dev"
  }
}
