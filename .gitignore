# Dependencias
node_modules/

# Variables de entorno
.env
.env.local

# Builds
dist/
build/
# Frontend compilado por el build raíz (npm run build lo regenera por completo en cada corrida)
server/client/

# Logs
*.log
npm-debug.log*

# Sistema
.DS_Store
Thumbs.db

# Uploads
server/uploads/*
!server/uploads/.gitkeep

# Configuración de deploy con secretos (AUDIT-001) — usar variables de entorno reales, no archivos
web.config
server/web.config
server/update-admin.js
*.tar.gz.backup

# Herramientas de diagnóstico temporal (eliminar después de verificar versión de Node en SmarterASP)
server/version-check.js
server/web.config.version-check
